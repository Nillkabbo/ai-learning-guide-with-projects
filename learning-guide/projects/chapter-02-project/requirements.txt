# Chapter 2 Project Requirements

# Token counting (OpenAI models)
tiktoken>=0.5.0

# Numerical operations for embeddings
numpy>=1.24.0

# Choose ONE of the following:

# Option 1: OpenAI (Cloud-based, requires API key)
openai>=1.0.0

# Option 2: Ollama (Local, no API key needed)
# ollama>=0.1.0
# Note: For Ollama, you'll need an embedding model:
# Run: ollama pull nomic-embed-text

